extends Node
## SignalBus — global autoload that decouples WebSocket messages from overlay UI.

signal standings_updated(text: String)
signal score_updated(text: String)
signal ticker_updated(text: String)
